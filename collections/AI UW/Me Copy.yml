info:
  name: Me Copy
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}/api/open-corporate-entries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
