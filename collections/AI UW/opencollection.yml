opencollection: 1.0.0

info:
  name: AI UW

request:
  auth:
    type: bearer
    token: "{{access_token}}"
  variables:
    - name: access_token
      value: ""
    - name: workflow_run_id
      value: ""
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
