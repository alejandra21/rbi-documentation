info:
  name: bank-statements
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/workflows/bank-statements/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
