info:
  name: withdrawals_1
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/workflows/withdrawals/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
