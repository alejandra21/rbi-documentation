info:
  name: manipulation-detections
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/workflows/manipulation-detections/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
