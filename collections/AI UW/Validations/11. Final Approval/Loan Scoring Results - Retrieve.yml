info:
  name: Loan Scoring Results - Retrieve
  type: http
  seq: 42

http:
  method: GET
  url: "{{base_url}}/api/workflows/loan-scoring-results/<id>/"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
