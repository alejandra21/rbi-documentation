info:
  name: Validations_1
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/api/workflows/validations/?phase=title_insurance"
  params:
    - name: phase
      value: title_insurance
      type: query
  body:
    type: json
    data: |-
      {
          "workflow_loan_summary": "{{workflow_id}}",
          "entity_name": "pepe garcia"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("phase_summary_phase_7", res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
