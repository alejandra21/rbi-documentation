info:
  name: photo-validations_1
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/workflows/collateral-review/photo-validations/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
