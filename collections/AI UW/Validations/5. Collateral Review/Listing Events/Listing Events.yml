info:
  name: Listing Events
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/workflows/collateral-review/listing-events/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
