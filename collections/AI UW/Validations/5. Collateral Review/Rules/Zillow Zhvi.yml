info:
  name: Zillow Zhvi
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/phase5-business-rules/?zipcode=33976"
  params:
    - name: zipcode
      value: "33976"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
