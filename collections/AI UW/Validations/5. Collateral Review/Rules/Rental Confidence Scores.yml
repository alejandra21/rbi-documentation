info:
  name: Rental Confidence Scores
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/phase5-business-rules/rental-confidence-scores/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
