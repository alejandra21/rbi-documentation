info:
  name: Score Buckets
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/phase5-business-rules/score-buckets/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
