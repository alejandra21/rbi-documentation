info:
  name: Market Stability Scores
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/phase5-business-rules/market-stability-scores/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
