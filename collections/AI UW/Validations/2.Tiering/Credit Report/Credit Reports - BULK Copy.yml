info:
  name: Credit Reports - BULK Copy
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/workflows/credit-reports/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
