info:
  name: Validations_1
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/workflows/validations/?phase=credit_review"
  params:
    - name: phase
      value: credit_review
      type: query
  body:
    type: json
    data: |-
      {
          "workflow_loan_summary": "{{workflow_id}}",
          "entity_name": "pepe garcia"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("phase_summary_phase_8", res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
