info:
  name: Workflows_1
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/workflows/:id/"
  params:
    - name: id
      value: "{{workflow_run_id}}"
      type: path
  body:
    type: json
    data: |-
      {
          "phase": "PHASE_1",
          "loan_id": 382,
          "callback_url": "https://webhook.site/95a17655-ff33-4937-a377-0aa128085710"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
