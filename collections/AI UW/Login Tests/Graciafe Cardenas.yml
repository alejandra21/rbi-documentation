info:
  name: Graciafe Cardenas
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/token/"
  body:
    type: json
    data: |-
      {
          "username": "graciafe@rbialliance.com",
          "password": "fmUb*&5{]vG95!Ou"
      }

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("access_token", res.body.access);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
