info:
  name: Beatriz Tortolero
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/token/"
  body:
    type: json
    data: |-
      {
          "username": "beatriz@rbialliance.com",
          "password": ",9dbi6$*ry0@zoFn"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        // Parse the JSON response
        let response = res.getBody();

        // Check if 'token' exists in the response
        if (response.access) {
            // Save token to environment variable
        //     pm.collectionVariables.set("access_token", response.access);
            console.log("✅ Token saved to environment variable: jwt_token");
        } else {
            console.log("⚠️ No 'token' found in the response body.");
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
