info:
  name: May Khoury
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/token/"
  body:
    type: json
    data: |-
      {
          "username": "May@Rbiallaince.com",
          "password": "x(KYDuO6z>kjLU?P"
      }

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("access_token", res.body.access);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
