info:
  name: OCR for Tables paddle 2
  type: http
  seq: 21

http:
  method: POST
  url: "{{base_url}}/api/ocr/extract-tables-paddle/"
  body:
    type: json
    data: |-
      {
          "url": "https://rbialliance.retool.com/api/file/f94a4665-2f01-408f-96b9-0a45c2319a3a"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // Parse the JSON response
        let response = res.getBody();

        // Check if 'token' exists in the response
        if (response.access) {
            // Save token to environment variable
        //     pm.collectionVariables.set("access_token", response.access);
            console.log("✅ Token saved to environment variable: jwt_token");
        } else {
            console.log("⚠️ No 'token' found in the response body.");
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
