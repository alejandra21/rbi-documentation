info:
  name: Tier Validations_1
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/workflows/tier-validations/"
  body:
    type: json
    data: |-
      {
          "workflow_loan_summary": "{{workflow_id}}",
          "entity_name": "pepe garcia"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("phase_summary_phase_2", res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
