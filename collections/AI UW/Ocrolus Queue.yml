info:
  name: Ocrolus Queue
  type: http
  seq: 25

http:
  method: GET
  url: "{{base_url}}/api/workflows/ocrolus-queue/?status=pending"
  params:
    - name: status
      value: pending
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
