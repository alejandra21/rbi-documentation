info:
  name: Loans - Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/panel/loans"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: page
      value: "1"
      type: query
      enabled: true
    - name: limit
      value: "10"
      type: query
      enabled: true
    - name: search
      value: ""
      type: query
      enabled: false
    - name: loanAddresses
      value: "true"
      type: query
      enabled: false
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
