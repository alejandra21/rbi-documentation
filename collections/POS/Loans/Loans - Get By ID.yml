info:
  name: Loans - Get By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/panel/loans/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "4143"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
