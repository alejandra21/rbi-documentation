info:
  name: fci - status history (paystring)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/loan/:loanId/payments?servicer_source=fci&record_type=status&order=desc"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: loanId
      value: "1687"
      type: path
    - name: servicer_source
      value: fci
      type: query
    - name: record_type
      value: status
      type: query
    - name: order
      value: desc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
