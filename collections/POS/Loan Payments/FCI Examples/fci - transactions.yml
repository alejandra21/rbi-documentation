info:
  name: fci - transactions
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/loan/:loanId/payments?servicer_source=fci&record_type=transaction&limit=50"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: loanId
      value: "1687"
      type: path
    - name: servicer_source
      value: fci
      type: query
    - name: record_type
      value: transaction
      type: query
    - name: limit
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
