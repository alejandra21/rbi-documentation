info:
  name: fci - payoffs
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/loan/:loanId/payments?servicer_source=fci&payment_type=Payoff"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: loanId
      value: "1847"
      type: path
    - name: servicer_source
      value: fci
      type: query
    - name: payment_type
      value: Payoff
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
