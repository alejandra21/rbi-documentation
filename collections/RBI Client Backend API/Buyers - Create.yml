info:
  name: Buyers - Create
  type: http
  seq: 44

http:
  method: POST
  url: "{{base_url}}/panel/create-buyer"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  body:
    type: json
    data: |-
      {
        "name": "Test Buyer"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
