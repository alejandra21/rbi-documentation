info:
  name: Payment Links - Verify
  type: http
  seq: 12

http:
  method: POST
  url: "{{base_url}}/payment-links/verify"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
