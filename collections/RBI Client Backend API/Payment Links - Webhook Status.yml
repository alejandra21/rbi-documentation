info:
  name: Payment Links - Webhook Status
  type: http
  seq: 14

http:
  method: POST
  url: "{{base_url}}/payment-links/webhook-status"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
