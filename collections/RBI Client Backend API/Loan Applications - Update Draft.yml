info:
  name: Loan Applications - Update Draft
  type: http
  seq: 7

http:
  method: PUT
  url: "{{base_url}}/loan-applications/draft"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": 1,
        "applicationId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
