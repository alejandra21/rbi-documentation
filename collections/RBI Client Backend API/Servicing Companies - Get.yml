info:
  name: Servicing Companies - Get
  type: http
  seq: 45

http:
  method: GET
  url: "{{base_url}}/panel/servicing-companies"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
