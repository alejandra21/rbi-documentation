info:
  name: Loan Applications - Count
  type: http
  seq: 24

http:
  method: GET
  url: "{{private_url}}/loan-applications/count"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
