info:
  name: Servicing Companies - Create
  type: http
  seq: 46

http:
  method: POST
  url: "{{base_url}}/panel/servicing-company"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  body:
    type: json
    data: |-
      {
        "name": "Servicing Co"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
