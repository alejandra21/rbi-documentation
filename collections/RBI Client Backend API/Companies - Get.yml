info:
  name: Companies - Get
  type: http
  seq: 38

http:
  method: GET
  url: "{{base_url}}/panel/companies"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
