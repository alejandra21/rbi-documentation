info:
  name: Payment Links - Create
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/payment-links"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
