info:
  name: User - Get User
  type: http
  seq: 17

http:
  method: GET
  url: "{{base_url}}/user/"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
