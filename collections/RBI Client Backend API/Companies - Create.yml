info:
  name: Companies - Create
  type: http
  seq: 39

http:
  method: POST
  url: "{{base_url}}/panel/create-company"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  body:
    type: json
    data: |-
      {
        "name": "Test Company"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
