info:
  name: User - Refresh
  type: http
  seq: 16

http:
  method: GET
  url: "{{private_url}}/user/refresh"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
