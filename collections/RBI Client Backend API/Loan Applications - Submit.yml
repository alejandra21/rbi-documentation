info:
  name: Loan Applications - Submit
  type: http
  seq: 26

http:
  method: POST
  url: "{{base_url}}/loan-applications/submit"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  body:
    type: json
    data: |-
      {
        "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
