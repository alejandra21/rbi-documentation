info:
  name: Loan Officers - Get All
  type: http
  seq: 33

http:
  method: GET
  url: "{{base_url}}/loan-officers-management"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
