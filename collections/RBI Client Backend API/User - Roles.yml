info:
  name: User - Roles
  type: http
  seq: 22

http:
  method: GET
  url: "{{base_url}}/roles"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
