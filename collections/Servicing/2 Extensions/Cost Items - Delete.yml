info:
  name: Cost Items - Delete
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{base_url}}/panel/extensions/{{extension_id}}/cost-items/{{cost_item_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
