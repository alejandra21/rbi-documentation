info:
  name: Extensions - Summary
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/panel/extensions/summary"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
