info:
  name: EPD Rules
  type: http
  seq: 6

http:
  method: GET
  url: "{{servicing_api}}/epd/rules"
  headers:
    - name: x-api-key
      value: "{{read_api_key}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
