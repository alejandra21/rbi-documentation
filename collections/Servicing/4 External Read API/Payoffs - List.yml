info:
  name: Payoffs - List
  type: http
  seq: 4

http:
  method: GET
  url: "{{servicing_api}}/payoffs"
  headers:
    - name: x-api-key
      value: "{{read_api_key}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
