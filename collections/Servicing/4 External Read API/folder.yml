info:
  name: 4 External Read API
  type: folder
  seq: 4

request:
  auth: none

docs:
  content: |
    The `servicing:read` surface — read-only, GETs only, consumed
    service-to-service. Uses `read_api_key`.
  type: text/markdown
