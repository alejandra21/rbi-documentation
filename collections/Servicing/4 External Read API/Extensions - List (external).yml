info:
  name: Extensions - List (external)
  type: http
  seq: 1

http:
  method: GET
  url: "{{servicing_api}}/extensions"
  headers:
    - name: x-api-key
      value: "{{read_api_key}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
