info:
  name: Extensions - Config
  type: http
  seq: 3

http:
  method: GET
  url: "{{servicing_api}}/extensions/config"
  headers:
    - name: x-api-key
      value: "{{read_api_key}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
