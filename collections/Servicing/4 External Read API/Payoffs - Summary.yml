info:
  name: Payoffs - Summary
  type: http
  seq: 5

http:
  method: GET
  url: "{{servicing_api}}/payoffs/summary"
  headers:
    - name: x-api-key
      value: "{{read_api_key}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
