info:
  name: Loan - Servicing Fields
  type: http
  seq: 8

http:
  method: GET
  url: "{{servicing_api}}/loans/{{loan_id}}/servicing-fields"
  headers:
    - name: x-api-key
      value: "{{read_api_key}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
