info:
  name: read key on decision (must fail)
  type: http
  seq: 7

http:
  method: POST
  url: "{{servicing_api}}/extension-fee/decision"
  headers:
    - name: x-api-key
      value: "{{read_api_key}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "eventId": "evt-scope-test-002",
        "externalRecordId": "{{extension_id}}",
        "decision": "APPROVED"
      }
  auth: none

docs:
  content: The read-only key must never write. Refused.
  type: text/markdown

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 0
