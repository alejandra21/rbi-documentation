info:
  name: no key at all (must fail)
  type: http
  seq: 5

http:
  method: GET
  url: "{{servicing_api}}/extension-fee/{{extension_id}}/borrower-proof"
  auth: none

docs:
  content: |
    No credential whatsoever. Fails closed.

    Also worth trying: a valid admin JWT as a bearer. The external router is
    JWT-free by design and must not accept one.
  type: text/markdown

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 0
