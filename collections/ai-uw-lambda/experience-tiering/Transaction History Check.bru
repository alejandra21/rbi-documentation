meta {
  name: Transaction History Check
  type: http
  seq: 3
}

post {
  url: {{aiUwLambdaUrl}}/workflows/transaction-history-check/run
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "loan_id": {{loan_id}},
    "workflow_id": "{{workflow_id}}",
    "workflow_run_id": "{{workflow_run_id_phase_2}}",
    "phase": "PHASE_2"
  }
}
