meta {
  name: Calculation Underwriting Metrics
  type: http
  seq: 4
}

post {
  url: {{aiUwLambdaUrl}}/workflows/calculation-underwriting-metrics/run
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "loan_id": {{loan_id}},
    "workflow_id": "{{workflow_id}}",
    "workflow_run_id": "{{workflow_run_id_phase_6}}",
    "phase": "PHASE_6"
  }
}
