meta {
  name: Validations Good Standing
  type: http
  seq: 3
}

post {
  url: {{aiUwLambdaUrl}}/workflows/validations-good-standing/run
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "loan_id": {{loan_id}},
    "workflow_id": "{{workflow_id}}",
    "workflow_run_id": "{{workflow_run_id_phase_1}}",
    "phase": "PHASE_1"
  }
}
