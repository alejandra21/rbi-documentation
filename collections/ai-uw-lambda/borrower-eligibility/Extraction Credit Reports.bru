meta {
  name: Extraction Credit Reports
  type: http
  seq: 5
}

post {
  url: {{aiUwLambdaUrl}}/workflows/extraction-credit-reports/run
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "loan_id": {{loan_id}},
    "workflow_id": "{{workflow_id}}",
    "workflow_run_id": "{{workflow_run_id_phase_1}}",
    "phase": "PHASE_1"
  }
}
