meta {
  name: Builders Risk Extraction
  type: http
  seq: 4
}

post {
  url: {{aiUwLambdaUrl}}/workflows/builders-risk-extraction/run
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "loan_id": {{loan_id}},
    "workflow_id": "{{workflow_id}}",
    "workflow_run_id": "{{workflow_run_id_phase_9}}",
    "phase": "PHASE_9"
  }
}
